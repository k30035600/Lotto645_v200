# 정적 HTML → GitHub Pages (GitHub 스타터 static.yml과 동일 구조)
# Settings → Pages → Source: GitHub Actions
# Environments → github-pages → 배포 브랜치에 main 포함 필수(기본 브랜치만 허용이면 main이 막힐 수 있음)
# 저장소 Settings → Actions → General → Workflow permissions: Read and write
name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  actions: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v5
        with:
          path: '.'
          # .source/, .nojekyll 등 점(.) 경로 포함 (기본은 제외됨)
          include-hidden-files: true
      - id: deployment
        uses: actions/deploy-pages@v5
